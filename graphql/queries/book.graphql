query Book($FEN: String!) {
  book(FEN: $FEN) {
    id
    vkey
    vmove
    vscore
    vwin
    vdraw
    vmemo
    chessdbscore
    vlost
    vFEN
    vvalid
    isfetched
    countFetched
    chessdbInfo
  }
}
